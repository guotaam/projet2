{% extends 'base.html.twig' %}



{% block body %}
<a class ="btn btn-succes" href="{{ path('vehicule_new')}}">Ajouter une voiture</a>

<table class="table">
<thead>
<tr>
 <th>Vehicule</th>
 <th>Membre</th>
 <th>Date de depart</th>
 <th>Date de fin</th>
 <th>Prix</th>
 
 <th>Date_enregistrement</th>
</tr>

</thead>
<tbody>
{% for membre in membres %}
<tr>
<td>{{ vehicule.titre}}</td>
<td>{{ vehicule.marque}}</td>
<td>{{ vehicule.modele}}</td>
<td>{{vehicule.description}}</td>
<td><img src="{{ vehicule.photo}}"></td>
<td>{{ vehicule.prixJournalier}}</td>
<td>{{ vehicule.dateEnregistrement|date('d/m/Y à H:i:s')}}</td>
 <td> <a href="{{ path('vehicule_edit',{ id:vehicule.id }) }}" class="btn btn-primary"> Modifier</a></td> 
 <td><a onclick="return confirm('voulez vous supprimer')" href="{{ path('vehicule_delete',{ id:vehicule.id }) }}" class="btn btn-primary">Supprimer  </a></td> #}
</tr>
{% endfor %}


</tbody>






</table>

{% endblock  %}