{% extends "base.html.twig" %}

{% block body %}

{% for message in app.flashes('sucess') %}
<div class="alert alert-success text-center">{{ message }}</div>
   
{% endfor %}

<a href="{{ path('admin_new_vehicules') }}" class="btn btn-success">Ajouter un vehicule</a><br><br>
<table class= "table table-bordred text-center">


<thead>
 <tr>
  {% for data in colonnes%}
   
     <th>{{ data }}</th>
    
  {% endfor %}
  
  <th>Editer</th>
  <th>Supprimer</th>
 </tr>

</thead>
<tbody>
 {% for item in vehicules %}
   
     <tr>
     <td>{{item.id}}</td>
     <td>{{item.titre}}</td>
     <td>{{item.marque}}</td>
     <td>{{item.modele}}</td>
     <td>{{item.description}}</td>
     <td><img src="{{item.photo}}" style="width: 150px;"></td>
     <td>{{item.prixJournalier}}</td>
     <td>{{ item.dateEnregistrement|date('d/m/Y à H:i:s') }}</td>
     
     <td><a href="{{path('admin_edit_vehicules',{id: item.id})}} " class ="text-dark"><i class ="fas fa-edit"></i></a></td>
     <td><a onclick="return confirm('voulez voussupprimer cet article?')" href="{{path('admin_delete_vehicules',{id: item.id})}}" class ="text-dark"><i class ="fas fa-trash"></i></a></td>
     
    
     </tr>
    
  {% endfor %}



</tbody>




</table>






{% endblock %}